interface:
  display_name: "Retry via Local Proxy"
  short_description: "Retry failed fetches and HTTP(S) Git clones"
  default_prompt: "Use $retry-via-local-proxy to retry this failed read-only fetch or timed-out HTTP(S) Git clone through a detected local proxy."
