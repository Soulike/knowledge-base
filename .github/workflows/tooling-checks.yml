name: Tooling checks

on:
  workflow_call:

permissions:
  contents: read

jobs:
  check:
    name: Tooling checks
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          version: latest

      - name: Set up latest Node.js LTS
        uses: actions/setup-node@v7
        with:
          node-version: "lts/*"
          check-latest: true
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install fixed gh-aw compiler
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh extension install github/gh-aw --pin v0.88.2

      - name: Check generated Agentic workflows
        env:
          GH_TOKEN: ${{ github.token }}
        run: pnpm agentic:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Check formatting
        run: pnpm format:check

      - name: Check internal Markdown links
        run: pnpm links:check

      - name: Check prompt links
        run: pnpm prompt-links:check

      - name: Check knowledge document format
        run: pnpm --dir .github/scripts/check-knowledge-format check "${{ github.workspace }}/knowledge"

      - name: Test
        run: pnpm test
